extends:
  - packages/prelude/lefthook.yml
  - packages/trpc/lefthook.yml

pre-commit:
  parallel: true
  commands:
    root-rome:
      glob: '*'
      run: npx zx ./scripts/rome-staged.js {staged_files}

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit $1
